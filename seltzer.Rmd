---
title: "R Notebook"
output: html_notebook
---
```{r}
install.packages(c("httr", "jsonlite"))
```

```{r}
library(httr)
library(jsonlite)
```

```{r}
res = GET("https://maps2.dcgis.dc.gov/dcgis/rest/services/FEEDS/MPD/MapServer/2/query?where=1%3D1&outFields=*&outSR=4326&f=json", 
          query = list(SHIFT = 'EVENING | DAY| MIDNIGHT', REPORT_DATE > ))
data = fromJSON(rawToChar(res$content))
```

